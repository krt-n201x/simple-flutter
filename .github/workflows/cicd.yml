name: CICD
on:
  push:
    branches:
    - '*'
jobs:
  build:
    name: build flutter
    runs-on: window
    env:
      JAVA_VERSION: "11.0"
      FLUTTER_VERSION: "2.8.1"
    steps:
      - uses: actions/checkout@v2